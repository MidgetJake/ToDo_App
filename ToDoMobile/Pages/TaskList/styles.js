import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    root: {
        backgroundColor: '#fff',
        height: '100%',
    },
    bottomSect: {
        marginLeft: 16,
        marginRight: 16,
        paddingBottom: 16,
    },
    bottomButton: {
        marginBottom: 16,
    },
    bottomText: {
        textAlign: 'center',
        width: '100%',
        margin: 8,
    }
});

export default styles;
