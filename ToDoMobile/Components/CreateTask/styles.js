import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
  root: {
    padding: 16,
  },
  title: {
    fontSize: 18,
    textAlign: 'center',
    marginBottom: 16,
  }
});

export default styles;
