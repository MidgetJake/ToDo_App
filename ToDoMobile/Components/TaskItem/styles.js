import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
  task: {
    //width: '100%',
    margin: 4,
    marginLeft: 16,
    marginRight: 16,
    overflow: 'hidden',
  },
  inner: {
    padding: 16,
    backgroundColor: '#fff',
    flexDirection: 'row',
  },
  text: {
    color: '#444',
    marginLeft: 16,
  }
});

export default styles;
